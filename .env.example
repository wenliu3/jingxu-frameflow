# ============================================================
# 本文件已不再是必需的——所有服务参数（ComfyUI 地址、DeepSeek、
# ModelScope、视频画质）都在前端右上角「服务配置」弹窗里填，
# 保存后写入项目根目录 service_config.json，改完立即生效。
#
# 这个文件整体可以删除，或留空。仅当你习惯用环境变量给命令行
# （video_agent.py 等）兜底时才需要它，且优先级低于面板配置：
#   面板保存的值 > 这里的环境变量 > 代码内置默认值
# ============================================================

# ↓↓↓ 以下全部为可选的兜底项，面板里填了就以面板为准 ↓↓↓

# --- 文本模型 · DeepSeek ---
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-flash
# DEEPSEEK_MAX_TOKENS=16000

# --- 图片模型 · ModelScope（云端 API，本地无需下载模型） ---
# 获取路径：modelscope.cn -> 个人中心 -> 访问令牌 -> 创建（勾选"大模型推理"）
# 前置条件：绑定阿里云账号并完成实名认证
# MODELSCOPE_API_TOKEN=ms-xxxxxxxxxxxxxxxx
# MODELSCOPE_IMAGE_MODEL=Tongyi-MAI/Z-Image-Turbo
# IMAGE_BASE_URL=https://api-inference.modelscope.cn/v1

# --- 图生视频画质档（ComfyUI + MiniMax H3）---
# H3_MEGAPIXELS=0.5
# H3_STEPS=4
# H3_LORA=minimax_h3_fl2v_turbo_4step_v1.0_768p_comfyui_bf16.safetensors

# --- ComfyUI 地址（一般在前端面板填；隧道地址 60 分钟一换）---
# COMFYUI_URL=https://xxxx.free.pinggy.net
